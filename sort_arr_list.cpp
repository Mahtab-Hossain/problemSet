#include <iostream>
#include <algorithm>

using namespace std;


int main(){
    int size,a;
    cout<<"insert size: ";
    cin>>size;
    int array[size];
    for(a=0;a<10;++size){
        cin>>array[a];

    }
    cout<<"inserted value :"<<array[a];
    cout<<"sorted values"<<"\n";
    

}